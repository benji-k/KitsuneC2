KITSUNEC2_WEBURL="https://127.0.0.1:7331/" //location of the backend service
KITSUNEC2_API_AUTH_TOKEN="2b409e3af5bb12ddd06b407980db9d99" //Authentication token to backend service. Should have the same value as the API_AUTH_TOKEN env variable in the backend service

NEXTAUTH_URL="https://127.0.0.1:3000/api/auth"
NEXTAUTH_SECRET="" //generate with openssl rand -base64 32
USERNAME="USER" //username to login the web-app
PASSWORD="PASS" //password to login the web-app
NODE_TLS_REJECT_UNAUTHORIZED=0 //Do not change, unless you have certificates that are signed by a trusted CA.